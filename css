body {
  background-color: #f7f7f7;
  font-family: Arial, sans-serif;
}

.container {
  max-width: 600px;
}

.card {
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

#message {
  font-size: 16px;
  color: #28a745;
}

.btn {
  width: 100%;
}
